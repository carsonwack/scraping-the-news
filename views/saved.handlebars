<header>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" id="to-home" href="/">Five-Thirty-Scraped</a>

    <div class="navbar" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="/saved" id="saved-articles">Saved Articles <span class="sr-only">(current)</span></a>
        </li>
      </ul>
    </div>
  </nav>
</header>


<div id="home">
  <div class="container">
    <div class="row">
      <div class="col-8 offset-2">
        <ul id="articles-list-saved">
          {{#each articles}}
          <li data-id="{{this._id}}" class="list-group-item">
            <a href="{{this.link}}">{{this.title}}</a>
            <button type="button" href="/remove/{{this._id}}" class="btn btn-outline-danger btn-sm" id="remove-button" data-id="{{this._id}}">Remove</button>
            <button type="button" class="btn btn-outline-primary btn-sm" id="notes-button" data-id="{{this._id}}">Notes</button>
          </li>
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="/js/index.js"></script>